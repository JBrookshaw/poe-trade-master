interface:
  display_name: "POE Trade Hideout"
  short_description: "Refresh POE trade URLs and click hideout"
  default_prompt: "Use $poe-trade-hideout on this Path of Exile trade URL and click Travel to Hideout if a live result is available."

dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Playwright MCP browser automation server"

policy:
  allow_implicit_invocation: true
